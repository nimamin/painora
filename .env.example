# Anthropic (AI intake on /chat)
ANTHROPIC_API_KEY=

# Supabase — new-style keys (2025+). Copy these straight from the
# dashboard: Project Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only secret key (sb_secret_…). Bypasses RLS — never expose to the browser.
# Optional for now; only needed for privileged writes/admin tasks.
SUPABASE_SECRET_KEY=
